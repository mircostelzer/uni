package model.shows;

import model.AbstractShow;

public class Film extends AbstractShow {

    public Film(String title, int year, String language, double basePrice) {
        super(title, year, language, basePrice);
    }
}
