#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    ifstream in("input.txt");

    in >> N;

    int dp[N];
    int a[N];

    for (int i = 0; i < N; i++) {
        in >> a[i];
    }

    
    ofstream out("output.txt");
    

    return 0;
}